/*

 * Object objeto
    * Tipo de dado estrutural
      * Propriedades (atributos) / 
      * Funcionalidades (métodos)

    { propriedade: <valor> }
*/

console.log({
  // atributos
  name: 'Mayk',
  idade: 36,

  // métodos
  andar: function() {
    console.log('Andando...')
  }
})
  // criando um objeto ao abrir chaves {}